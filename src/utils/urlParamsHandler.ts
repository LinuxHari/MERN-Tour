// import { TourCategories } from "../type"

type ListingParams = {
    destination: string;
    destinationType: string;
    tourType: string;
    startDate: string;
    endDate: string;
}

// export const listingUrlParamsHandler = ({destination, destionationType, tourType, startDate, endDate}:ListingParams) => {

// }